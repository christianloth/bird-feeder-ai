# Local secrets for the pipeline. Copy this file to `.env` and fill in.
# `.env` is gitignored; keep your secrets out of YAML and out of git.

# Telegram Bot API token from @BotFather. Required when
# notifications.telegram.enabled is true in config/config.yaml.
TELEGRAM_BOT_TOKEN=
